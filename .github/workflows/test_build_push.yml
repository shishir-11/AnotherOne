name: Deploy Multidocker
on:
    push:
        branches:
            - master

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/react-test -f ./client/Dockerfile.dev ./client
            - run: docker run -e CI=true ${{ secrets.DOCKERHUB_USERNAME }}/react-test npm test
    
    login:
        needs: test
        runs-on: ubuntu-latest
        steps:
            - 
                name: Login to dockerhub
                uses: docker/login-action@v3
                with:
                    username: ${{ secrets.DOCKERHUB_USERNAME }}
                    password: ${{ secrets.DOCKERHUB_TOKEN }}
    
    build:
        needs: login
        runs-on: ubuntu-latest
        steps:
            - run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/multi-client-10-14 ./client
            - run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/multi-nginx-10-14 ./nginx
            - run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/multi-server-10-14 ./server
            - run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/multi-worker-10-14 ./worker
       
            - run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/multi-client-10-14
            - run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/multi-nginx-10-14
            - run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/multi-server-10-14
            - run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/multi-worker-10-14